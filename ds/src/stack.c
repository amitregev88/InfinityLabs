
struct stack
{
    size_t size;
    void *stack;
    size_t capacity;
    size_t size_of_element;
};


typedef struct stack stack_ty;

stack_ty *StackCreate(size_t capacity, size_t size_of_element)

stack_ty * 
	
	stack_ty * = (stack_ty *) malloc(sizeof 

{





}
